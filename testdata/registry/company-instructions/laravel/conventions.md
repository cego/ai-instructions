# Laravel Conventions

- Use Form Requests for validation
- Follow Laravel naming conventions for routes, controllers, models
- Use Eloquent relationships instead of raw queries
- Keep controllers thin, move logic to services or actions
