# Eloquent Patterns

- Define fillable or guarded properties on all models
- Use scopes for reusable query logic
- Eager load relationships to avoid N+1 queries
- Use model events sparingly, prefer observers for complex logic
