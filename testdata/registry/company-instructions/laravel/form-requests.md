# Form Requests

- Create a Form Request for every store/update action
- Use authorize() method for authorization logic
- Keep validation rules in the rules() method
- Use custom messages for user-facing errors
