# Nuxt UI Components

- Use Nuxt UI components instead of building custom ones
- Follow the component prop patterns from Nuxt UI docs
- Use the built-in color and variant system
- Leverage the form validation integration
